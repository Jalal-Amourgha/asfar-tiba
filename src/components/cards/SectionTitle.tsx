const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h1 className="text-4xl text-primary font-semibold text-center mb-10">
      {title}
    </h1>
  );
};

export default SectionTitle;
